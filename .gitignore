# Segredos — NUNCA versionar (não são usados no site estático, mas por segurança)
.env
*.key

# Cloudflare Worker (proxy): estado local e segredos de dev
proxy/.wrangler/
proxy/.dev.vars
node_modules/

# SO / editores
.DS_Store
.idea/
.vscode/

# Python (ferramentas locais de teste, se usadas)
__pycache__/
*.pyc
.pytest_cache/
